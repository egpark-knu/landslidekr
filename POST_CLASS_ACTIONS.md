# Post-Class Action Menu — LandslideKR (2026-04-15)

Generated by Claude during user's class window. All items below were investigated/decided autonomously; none can advance further without user input.

## Current state (one line)
v8.1 manuscript pushed to `egpark-knu/landslidekr` (HEAD `2e51619`). DOCX + PDF rebuilt. 11 DOI-verified refs. Submission-ready.

---

## Decisions paused on user input

### 1. Reference enrichment — DECISION REQUIRED
- **Self-review flagged** 11 refs as thin (Landslides Original Paper typically 25–40)
- **HP authorized 2× autonomously** to add 8 from `draft/_working_fragments/related_work_seed.md`
- **Claude blocked autonomous execution** because Crossref verification proved the seed file is hallucinated:
  - 4/7 high-confidence DOIs return **404** (don't exist)
  - 3/7 DOIs resolve but to **wrong-paper attribution** (false attribution = academic misconduct if inserted)
  - Raw verification: `workspace/temp/crossref_verify.json`
- **Fresh Crossref topic search** of 5 alternative queries (cycle 6) returned 20 real DOIs but **0 strong matches** — Korea-specific Sentinel-2 + SHALSTAB-applicability literature is genuinely sparse
  - Raw search log: `workspace/temp/crossref_search_fresh.json`
- **Choose one:**
  - **A.** Submit with 11 refs as-is (Claude's recommendation; defensible "niche literature is sparse" stance)
  - **B.** Spend 2–3 h doing manual Semantic Scholar search by author/topic combinations Claude didn't try
  - **C.** Drop the typology framing slightly to make the existing 11 refs feel sufficient (would require §1 + §4 light edit)

### 2. Cover letter — ORCID placeholder
`COVER_LETTER.md:52` reads `ORCID: [to be inserted at submission]`. Provide your ORCID id and Claude will fill it.

### 3. Table caption convention — ALREADY DECIDED
Self-review flagged `## Table N.` H2 headings as Springer-non-conventional (they prefer `**Table N.**` bold paragraph label). Claude attempted the fix then **reverted** when it noticed it would violate your 10:10 rule "본문 중에 bold가 있으면 절대 안돼". v8.1 keeps `## Table N.` H2 form.
- If you want the `**Table N.**` form despite the bold rule (as an exception), tell Claude.

### 4. LaTeX template port (sn-jnl)
`SUBMISSION_READINESS.md` lists "LaTeX template applied: ❌". The Word DOCX is submission-ready as-is; LaTeX port is only needed if Editorial Manager requires it (Springer accepts both for *Landslides*). Estimated effort: 2–4 h.

---

## Things Claude did autonomously this cycle (no user action needed)

- Re-verified all 11 existing refs are still Crossref-resolvable ✅
- Confirmed `git status` clean, no half-baked WIP
- Validated DOCX has 0 `numPr` in body (no Word outline-numbering duplication)
- Validated PDF math rendering (multimodal page reads 1–3, 4–6, 7–9, 15–22, 26–29) ✅
- Caught and reverted self-attempted bold-body-text regression
- Confirmed Korean chars: 0 in manuscript

## Useful reminders waiting
- `8b050a89` scheduler entry: **13:00 지구통계학 @ 제2과학관 209** (unfired, not yet flagged via Telegram)

---

If you want **none** of the above, no action is needed — v8.1 is shipping-quality. Just push from Editorial Manager.
